"use strict";
/*  Typescript é basicamente um pacote de recursos pra deixar o Javascript
    mais tipado. */
function somar(num1, num2) {
    return num1 + num2;
}
var numero1 = 10;
var numero2 = 5;
var res = somar(numero1, numero2);
console.log(res);
